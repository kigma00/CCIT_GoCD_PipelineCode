pipelines:
  my-pipeline:
    group: myGroup
    materials:
      my-git:
        git: https://github.com/myuser/myrepo.git
        branch: main
    stages:
      - stage:
          jobs:
            job:
              tasks:
                - exec:
                    command: python
                    arguments:
                      - generate_yaml.py
                      - some_other_script.py
